version: "2"
sql:
    - schema: "migrations/"
      queries: "internal/repository/queries/"
      engine: "postgresql"
      gen:
          go:
              package: "repository"
              out: "internal/repository"
              emit_json_tags: true
